rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // REGLAS TEMPORALMENTE COMPLETAMENTE ABIERTAS PARA CREAR PRIMER ADMIN
    match /{document=**} {
      allow read, write: if true; // COMPLETAMENTE ABIERTO TEMPORALMENTE
    }
  }
}